version: 2

builds:
  - id: sshq
    main: ./cmd/sshq
    binary: sshq
    ldflags:
      - -s -w
      - -X github.com/shayuc137/sshq/internal/version.Version={{.Version}}
      - -X github.com/shayuc137/sshq/internal/version.Commit={{.ShortCommit}}
      - -X github.com/shayuc137/sshq/internal/version.Date={{.Date}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  # Version-less asset names keep releases/latest/download/ URLs permanent;
  # the version lives in the release tag.
  - ids: [sshq]
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
